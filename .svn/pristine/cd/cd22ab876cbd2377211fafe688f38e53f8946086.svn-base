package com.qbao.store.controller.app;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.ResponseBody;

import com.qbao.store.core.web.converter.Result;

/**
 * 用户控制器
 * @author yewenhai
 *
 */
@Controller
@RequestMapping("/app/user")
public class UserController extends BaseController {

    /**
     * 获取用户名
     * @return
     */
    @RequestMapping("/selectUserName")
    @ResponseBody
    public Result selectUserName() {
//        UserDetails user = this.getCurrentUser();
//        if (null == user) {
//            return Result.error("获取用户名失败", Result.RESPONSE_CODE_HAS_ERROR);
//        }
        return Result.success(null);
    }

}
